import org.threeten.bp.LocalDate;

CREATE TABLE entry(
    id TEXT PRIMARY KEY,
    date TEXT as LocalDate NOT NULL,
    position INTEGER UNIQUE NOT NULL
);

clear:
DELETE FROM entry;

insert:
INSERT INTO entry(id, date, position)
VALUES ?;

entryPreview:
SELECT id, date FROM entry ORDER BY date;
